{
  "name": "climap-id",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0"
  },
  "scripts": {
    "start": "echo 'React start script will run here after deployment'",
    "build": "echo 'React build script will run here after deployment'"
  }
}
